/**
 * API module - re-exports from api/ for backward compatibility.
 * Implementation is split in api/ (client, auth, users, restaurants, orders, cart, promotions, menus).
 */
export * from './api';
export { default } from './api';
