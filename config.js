// Configuration de l'application
export const config = {
  // URL de votre serveur Express/MongoDB
  API_BASE_URL: 'http://localhost:3000/api', // ⚠️ CHANGEZ CETTE URL !

  // Autres configurations
  APP_NAME: 'Good Food',
  VERSION: '1.0.0',

  // Timeout pour les requêtes API (en millisecondes)
  API_TIMEOUT: 10000,
};
